# Changelog

本文档记录所有值得用户关注的变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Added

- 视频播放倍速新增 2.25x、2.5x 和 3x 档位。

### Fixed

- 修复自动处理未索引图片时可能反复弹出成功提示的问题。
- 修复在素材库外部新建并重命名文件夹后，应用内可能残留多个“未命名文件夹”的问题。

## [0.6.3] - 2026-06-03

### Fixed

- 修复浏览器采集扩展的区域截图、元素截图、可视截图和批量收藏可能未记录当前网页 URL 的问题。
- 优化高分辨率图片预览弹窗的缩略条和相邻图片预热，减少打开大图时的卡顿。
- 优化批量导入完成后的提示，成功时不再弹出通知，仅在失败时提醒。

## [0.6.2] - 2026-05-22

### Added

- 主界面新增导入、AI 分析和向量计算的轻量进度条，并支持取消进行中的任务。

### Changed

- 小红书采集不再注入页面右键菜单，改用浏览器原生右键菜单与快捷采集入口，降低对页面结构的影响。
- 浏览器采集扩展不再通过页面主上下文兜底取图，减少对目标网页运行环境的干扰。

### Fixed

- 修复 Markdown 文件无法导入，以及部分 DOCX 缩略图显示为空白的问题。
- 修复从子文件夹双击打开图片预览时，顶部标题显示为文件夹名的问题。
- 修复浏览器采集扩展 Option/Alt 快捷发送时未弹出目标文件夹选择器的问题。
- 优化导入任务的请求校验，避免异常导入数据被静默处理。

## [0.6.1] - 2026-05-13

### Added

- 文档文件支持生成统一的首页缩略图，便于在素材网格中辨认 Word、RTF、ODT、HTML 和 PPTX 文档内容。
- DOCX 文件缩略图改用高保真页面渲染，保留更多原文档样式。

### Changed

- 优化视频播放栏为更紧凑的单行控制样式，并保留倍速选择。
- 文件详情中的视频预览改为静态缩略图，不再直接播放视频。
- 视频播放栏的跳转输入改为分秒时间码，便于按时间定位。

### Fixed

- 修复 Word 文档缩略图可能显示乱码或中文方框的问题。
- 修复视频预览底部缩略条只显示 MP4 兜底图标的问题。
- 修复视频进度悬停预览快速移动时不够跟手、不会持续刷新且可能显示旧帧的问题，并缩小预览尺寸。
- 修复视频音量滑块圆点半透明导致与蓝色进度重叠的问题。
- 优化浏览器扩展拖拽收藏时切换目标文件夹的响应速度。
- 降低图片预览缩略条一次性打开大量文件时的内存占用。
- 优化导入、AI 分析和视觉索引任务完成后的内存释放。

## [0.6.0] - 2026-05-12

### Fixed

- 优化素材网格单击选中文件的响应速度，减少点击后的迟滞感。
- Toast 通知支持在右上角直接关闭。
- 修复展开筛选栏时顶部工具栏轻微上移，并收紧筛选栏与素材网格的间距。
- 修复素材库下拉菜单可能被右侧内容遮挡，并增强菜单边界识别。
- 修复小红书页面内右键发送后，后续页面可能无法再次打开右键菜单的问题。
- 修复重启应用后仍保留文件类型、标签和主色等筛选条件的问题。

## [0.5.11] - 2026-05-11

### Added

- 文件夹详情会显示当前文件夹及子文件夹内文件的总大小。
- 回收站顶部会显示当前占用的总存储空间。

### Changed

- 视频详情页和全屏预览的倍速选择统一为弹出菜单。

### Fixed

- 修复 macOS 主窗口顶部出现一条浅色边线的问题。
- 修复小红书页面内右键发送不弹出目标文件夹选择器、菜单未立即关闭的问题。
- 优化视频全屏预览的倍速选择和左右跳转行为。

## [0.5.10] - 2026-05-10

### Fixed

- test超时问题

## [0.5.9] - 2026-05-10

### Changed

- AI 重命名遇到重名文件时改用简短数字序号，避免文件名出现内部时间戳后缀。

### Fixed

- 修复 pixiv 采集时作者可能被识别为当前登录账号、原图后缀猜错导致 404，并减少 pximg 取图时的页面 CORS 报错噪音。
- 浏览器采集扩展拖拽图片到目标文件夹后会立即收起浮层，发送继续在后台完成。
- 修复拖拽文件夹排序时插入提示线导致文件夹列表轻微晃动的问题。
- 修复不支持像素解码的图片格式在自动 AI 分析和自然语言搜索索引中仍被处理的问题，手动 AI 分析会提示具体不支持的格式。
- AI 分析成功完成时不再弹出成功提示，减少操作打断。

## [0.5.8] - 2026-05-10

### Changed

- 调整左侧栏快捷视图与文件夹树的行高和缩进，文件夹列表不再显示数量。
- 调整桌面端主界面顶部布局，移除独立顶部栏，并将素材库、导入、设置和搜索工具并入三栏栏目顶部。
- 微调桌面端顶部工具栏与左侧素材目录分区间距，让列表区域更紧凑。
- 主内容区新增本次运行内的文件夹/快捷视图后退、前进导航。
- 优化三栏顶部控件对齐，避免 macOS 红绿灯遮挡素材库切换，并移除详情栏顶部删除入口。

### Fixed

- 修复 macOS 收起左侧栏后，展开、后退和前进等顶部按钮可能被窗口红绿灯遮挡的问题。
- 优化浏览器采集扩展拖拽到目标文件夹列表时的自动滚动手感，减少误触发向下滚动。
- 修复 Windows 上部分图片拖拽预览生成后可能占用源文件的问题。
- 修复浏览器采集扩展图标打开连接状态弹窗、无法进入页面内采集面板的问题。
- 修复浏览器采集扩展右键发送时不弹出目标文件夹选择器的问题。
- 修复 pixiv 图片采集时页面可能出现已渲染像素复用失败提示的问题。
- 修复 pixiv 图片拖拽发送到拾光时可能因热链保护返回 403 的问题。
- 修复小红书图片右键发送时 CDN 拒绝重复取图导致发送失败的问题，并通过页面 URL 推导高清候选图。
- 移除浏览器采集扩展隐藏 iframe 取图路径，避免小红书等站点把图片 URL 导航识别为浏览器下载。

## [0.5.7] - 2026-05-09

### Added

- 浏览器采集扩展增强小红书、Pinterest、Behance、Dribbble、ArtStation、pixiv 及常见图片素材站的来源链接、标题和大图识别。
- 浏览器采集扩展支持从 Unsplash、Pexels、Pixabay、Flickr 和 Wikimedia Commons 透传网页元数据。
- 浏览器采集扩展支持拖拽图片到旁侧浮层中的目标文件夹，松手后直接收藏到对应文件夹。

### Changed

- 浏览器采集扩展改为 WXT 构建产物，加载未解压扩展时需选择 `.output/chrome-mv3` 目录。
- 浏览器采集改为由扩展侧读取并上传图片数据，优先复用浏览器已加载资源，桌面端不再根据远程链接重新下载图片。
- 浏览器采集扩展拖拽图片时改用小尺寸预览，减少遮挡目标文件夹浮层。

### Fixed

- 修复浏览器采集导入时只能记录来源链接、无法自动带入站点标题、作者、许可和标签的问题。
- 调整浏览器采集的网页标签落库方式，不再自动写入本地标签体系，改为追加到备注中保存。
- 修复浏览器采集扩展拖拽到文件夹列表时，长列表在拖拽过程中可能无法滚动的问题。
- 修复部分 HEIC 图片无法生成缩略图的问题。
- 修复 AVIF、HEIC 等图片拖拽时预览可能显示为文件图标的问题。

## [0.5.6] - 2026-05-06

### Fixed

- 测试问题

## [0.5.5] - 2026-05-06

### Removed

- 移除文件详情右侧的导出入口及对应导出命令。

### Fixed

- 修复浏览器扩展拖拽收藏浮层文字可能被网页全选复制带上的问题。
- 修复浏览器扩展发送前选择文件夹时，“浏览器采集”子文件夹层级显示丢失的问题。
- 修复素材文件正常保存后缩略图刷新规则不统一的问题。
- 优化中文拼音搜索的字段读取和排序，减少漏匹配与结果顺序不自然的问题。

## [0.5.4] - 2026-05-05

### Changed

- 图片预览与全屏预览的旋转工具只保留逆时针旋转入口。
- 调整侧栏收起入口位置，避免遮挡滚动条和面板拖拽区域。
- 加宽主内容区和左侧栏滚动条，减少与面板宽度调整手柄的误触。

### Fixed

- 修复拖拽素材时图片图标被拉伸铺满、拖动 PDF 等特殊文件可能崩溃的问题，并让非图片文件优先使用缩略图或文件类型图标。
- 修复侧栏文件夹展开后滚动条出现导致列表宽度跳动的问题，并让滚动条只在鼠标移入左侧栏时显示。
- 修复侧栏父文件夹和子文件夹之间的间距过窄问题。

## [0.5.3] - 2026-05-04

### Added

- 日志系统重构：集中管理日志配置，区分开发/生产环境级别，增加全局未捕获异常记录。
- 日志文件统一存放在应用数据目录（`userData/logs/`），不再写入系统默认的 `~/Library/Logs/`。
- 数据库写操作（创建、删除、重命名等）自动记录到日志，便于排查问题。
- 设置页面新增"打开日志目录"按钮，方便查看日志文件。

### Fixed

- 修复拖拽素材到 macOS Finder 无效的问题，现在可以正常拖出文件到桌面或文件夹。
- 修复拖拽素材后应用卡死的问题。
- 修复应用内拖拽素材到侧边栏文件夹无法移动的问题。

## [0.5.2] - 2026-05-02

### Added

- 搜索支持拼音匹配，输入全拼（如 "wangye"）或首字母缩写（如 "wy"）即可找到中文内容（如"网页"），文件名、文件夹名、标签名均生效。
- 支持从系统拖入整个文件夹导入，自动创建对应的文件夹层级结构，子文件夹中的素材会归入各自目录。

### Fixed

- 修复图片预览旋转的三个问题：旋转时不再有过渡动画（立即生效）、不再重置缩放级别、旋转后图片位置不再跳动。
- 修复卡片标签区域上方间距过大的问题，标签紧贴文件名和元信息显示。
- 缩小标签水平内边距，短文本标签的胶囊更紧凑。
- 修复 macOS 触控板双指捏合无法缩放图片预览的问题，禁用原生页面缩放以将手势正确传递给应用内缩放处理。
- AI 元数据分析失败时自动重试最多 3 次，间隔逐次递增，减少因网络波动或临时错误导致的分析失败。

### Changed

- 拖拽素材时统一使用 80×80 的固定大小预览图，不再跟随卡片实际尺寸；无缩略图的文件会显示文件图标和扩展名。
- 网格和自适应视图缩小图片预览区域高度，卡片整体更紧凑。
- 网格和自适应视图移除卡片背景色和鼠标悬停高亮效果。
- 网格视图移除加载中的模糊占位图。
- 网格卡片底部信息栏在无标签时不再预留标签空间，行高根据实际内容自动适配。

## [0.5.1] - 2026-05-01

### Changed

- 数据库改为使用 Drizzle 管理新表结构和迁移，新库会从新的基线结构开始创建。

### Fixed

- 修复素材库数据库版本号与实际结构不一致时可能启动失败的问题。

## [0.5.0] - 2026-05-01

### Added

- 搜索框新增独立 AI 搜索开关，默认使用文件名模糊搜索；文件夹和标签管理也支持缩写式模糊搜索。

### Changed

- 新用户首次启动时默认使用深色模式。
- 数据库时间记录改为毫秒级 UTC，并优化素材列表和文件夹路径查询的稳定性。
- 标签名称改为同一层级内唯一，不同父标签下可使用相同名称。

### Fixed

- 修复浏览器扩展发送前选择文件夹时重复显示“浏览器采集”的问题。

## [0.4.9] - 2026-04-30

### Added

- 浏览器采集文件夹支持在右键菜单中更换图标，并会记住选择。

### Changed

- 文件夹列表不再将“浏览器采集”作为固定置顶的系统文件夹，新建文件夹会出现在当前层级顶部。
- 侧栏快捷视图与文件夹图标列保持对齐，并作为同一个列表区域整体滚动。

### Fixed

- 修复深色模式下侧栏滚动条可能显示为亮色宽轨道的问题，并复用主内容区滚动条样式。

## [0.4.8] - 2026-04-30

### Added

- 图片预览与全屏预览新增左旋、右旋和中心反转操作，并收紧预览工具栏按钮间距。
- 视频详情和全屏预览新增进度悬停画面、前后 5 秒跳转、音量、播放速度、时间跳转和全屏控制。

## [0.4.7] - 2026-04-29

### Fixed

- 修复从小红书卡片采集图片时来源链接可能只记录当前 feed 页的问题。

## [0.4.6] - 2026-04-29

### Added

- 设置页本地自然语言搜索新增视觉模型下载入口，可通过 hf-mirror 选择下载目录并查看下载进度。

### Changed

- 最近素材库不再限制 8 条上限，列表超出可视区域时自动内部滚动。
- 导入图片时将颜色、缩略占位和内容指纹改为后台补全，减少大图导入等待时间。
- 导入后自动 AI 元数据分析扩展到文件选择、拖拽、剪贴板和浏览器采集入口，并自动合并短时间内的分析任务。

### Fixed

- 修复 PDF、PSD、视频等无内容指纹文件被修改后仍可能显示旧缩略图的问题。
- 统一 PSD、PDF、视频和普通图片的导入后处理规则，减少特殊格式缩略图和索引处理不一致的问题。
- 修复切换到包含视频的文件夹时，素材卡片可能短暂显示上一文件夹图片的问题。
- 修复回收站中视频文件不显示缩略图的问题。
- 修复拖拽导入多个本地视频时可能因整文件读入内存而白屏卡死的问题，并保留原文件名。
- 优化视频导入速度，避免对非图片文件执行图片视觉元数据分析。
- 修复导入视频时封面生成和文件夹切换刷新可能互相干扰，导致列表临时变空的问题。
- 修复多选素材删除时可能只删除单个文件的问题，并统一底部批量删除、右键菜单删除和 Delete 键删除当前选择的行为。
- 修复未配置本地视觉模型时，搜索框无法自动回退到文件名模糊搜索的问题。
- 修复切换本地视觉模型或重建视觉索引后，旧模型向量可能继续参与搜索的问题。

## [0.4.5] - 2026-04-28

### Added

- 浏览器采集扩展支持按偏好在发送前选择目标文件夹，并可识别多层级文件夹。

### Fixed

- 修复删除子文件夹后，回收站中已删除图片可能无法显示缩略图的问题。
- 修复采集 Pixiv 等图片时可能因主进程直连下载或缺少防盗链来源头而失败的问题。
- 修复主进程下载未跟随系统代理设置，导致部分图片站点采集超时的问题。
- 修复登录态或临时签名图片在后端无法下载时缺少浏览器侧兜底的问题。
- 修复浏览器侧兜底上传较大图片时可能被本地采集服务拒绝的问题。

## [0.4.4] - 2026-04-27

### Added

- 导入与素材库索引新增常见音频、办公文档、压缩包、HEIC/HEIF、JFIF/JPE 和更多视频格式的轻量支持。

### Fixed

- 修复在重复/相似视图删除素材后，列表可能沿用旧文件夹范围并临时刷新成不完整结果的问题。
- 修复从标签管理页进入重复/相似视图时可能沿用隐藏筛选或以图搜图状态，导致结果数量过少的问题。
- 修复右键菜单中复制或移动素材后菜单仍悬浮不消失的问题。

## [0.4.3] - 2026-04-27

### Changed

- 图片预览页将上一张/下一张按钮移到图片区域两侧，并降低默认视觉权重。

## [0.4.2] - 2026-04-26

### Added

- AI 元数据分析支持分别配置文件名、标签、备注和评价的开关与提示词，并可在导入任务完成后自动异步分析图片。
- 主界面支持收起和展开左右侧栏，并会记住折叠状态。

### Changed

- AI 元数据分析改为全局串行执行，重名文件会自动追加后缀避免覆盖。

### Fixed

- 修复多选素材后拖拽到文件夹时只移动单个素材的问题，并让多选拖拽预览显示数量。

## [0.4.1] - 2026-04-26

### Changed

- 全屏预览改为进入系统原生全屏，并精简为自动隐藏的浮动工具条，让图片本身占据主要视野。
- 图片预览页进一步统一为主界面风格，移除底部重复的文件信息行，并在切换图片时自动将当前缩略图定位回底部栏中。

## [0.4.0] - 2026-04-25

### Added

- 浏览器采集扩展新增元素截图，可像开发者工具选择元素一样高亮页面元素并收藏其截图。

### Changed

- 自动检查更新默认关闭，可在设置页手动开启。
- 素材网格和自适应视图改为连续流式浏览，不再显示底部分页。

### Fixed

- 修复导入后自动视觉索引可能重复计算或在前台补索引不及时的问题。
- 修复浏览器采集扩展在小红书图片链接遮罩上右键时不显示或无法发送到拾光的问题。

## [0.3.9] - 2026-04-25

### Fixed

- 修复自动更新打包

## [0.3.8] - 2026-04-25

### Added

- 设置页新增自动检查更新开关和手动检查更新入口，默认会在应用启动后自动检查新版本。
- Windows 和 AppImage 版本支持自动下载更新，macOS 与其他安装方式会提示前往最新 Release 手动更新。

## [0.3.7] - 2026-04-25

### Added

- 左侧快捷视图新增“重复/相似”，可先按内容哈希查找精确重复素材，再用视觉索引找相似图片。
- 搜索框支持拖入素材进行以图搜图，素材右键菜单也可直接按视觉相似度查找近似图片。

### Changed

- 以图搜图状态在搜索框内改为标签式展示，可通过关闭按钮或空输入退格快速移除。
- 素材详情中的文件夹位置改为显示相对路径，避免暴露本机绝对路径。

### Fixed

- 修复拖动素材到搜索框时可能触发系统原生拖拽，导致以图搜图不生效甚至界面卡住的问题。
- 修复从应用内复制图片后，外部文件夹无法直接粘贴为文件的问题，同时保留应用内粘贴元信息。

## [0.3.6] - 2026-04-25

### Changed

- 标签管理页进一步减少分割线、图标、提示文字和侧栏间距，标签列表与详情区改为更简洁的轻量布局。
- 回收站页面改为更轻量的行式列表，减少边框、卡片和说明文字的视觉干扰。
- 微调素材工具栏的信息显示图标尺寸，使其与其他图标视觉更统一。

### Fixed

- 回收站清空和永久删除操作会先弹出二次确认，并支持用 `Cmd/Ctrl+A` 全选回收站项目。

## [0.3.5] - 2026-04-24

### Added

- 浏览器采集扩展新增页面内采集面板，支持区域截图、可视范围截图、批量收藏、拖拽收藏开关和偏好设置。
- 浏览器采集扩展新增 `Alt + 右键` 快速收藏图片，并为快捷收藏、右键菜单、拖拽和批量收藏统一加入导入队列。

### Fixed

- 提升旧版本素材库数据库升级的可靠性，升级前会自动备份并按 schema 版本完成迁移。

## [0.3.4] - 2026-04-24

### Changed

- 标签管理页进一步统一为轻量分栏样式，收起说明文字和信息行，减少右侧详情区的视觉重量。
- 从标签管理页查看素材时会进入“全部素材 + 标签筛选”，并同步高亮左侧“全部素材”。
- 左侧栏文件夹列表改为更轻量的树形导航样式，减少选中态和计数的视觉重量。
- 设置页进一步统一为轻量行式布局，减少卡片、边框和说明文字，让通用、AI 与快捷键设置更贴近主界面风格。
- 素材筛选栏进一步简化为单行工具条，颜色改为轻量浮层选择，并移除重复的已选摘要。

## [0.3.3] - 2026-04-23

### Added

- 顶部新增当前素材库入口，可直接查看当前路径、在系统中打开素材库、重建索引，或更换素材库。
- 顶部素材库菜单会保留最近使用过的素材库，方便在不同素材库之间快速切换回来。
- 文件夹分区标题右侧新增 `+` 入口，可直接在当前素材库根目录创建真实文件夹。

### Changed

- 发布产物不再上传 macOS 和 Windows 的重复压缩包，并减少安装包内的冗余平台文件与未使用资源。
- 首次启动不再默认使用系统图片目录，必须先选择素材库文件夹后才能进入应用。
- 更换素材库继续采用重启切换流程，避免切换后的索引、监听和缓存状态混乱。
- 本地自然语言搜索默认改为使用 CPU、4 个推理线程和 `FG-CLIP Max Patches = 256`，降低首次配置后的资源占用和卡顿概率。
- 本地自然语言搜索运行时会在空闲后自动释放，关闭主窗口转入后台时也会主动停止视觉索引进程，减少常驻内存占用。
- 本地自然语言搜索的文本查询与图片索引现统一复用同一个后台 `utilityProcess`，并按需懒加载 text/image session，减少重复模型常驻带来的内存占用。
- 本地自然语言搜索设置中的模型入口改为直接打开模型使用说明，并补充 FG-CLIP2 与 Chinese-CLIP 的下载和配置文档。
- 应用内打开模型使用说明等外部网页时，改为统一交给系统默认浏览器处理。
- 素材列表筛选栏改为更轻量的一排式工具条，收起卡片和分隔线，颜色与高级条件按需展开。
- 主界面三栏进一步收紧视觉层级，淡化顶部与分栏线、边框和卡片感，并让素材选中态与右侧详情排版更清晰统一。
- 移除当前不可用的“仅收藏”筛选入口，避免与评分功能混淆。
- 删除文件夹后改为以“文件夹条目”进入应用内回收站，可在回收站恢复，当前会话内也可继续通过 `Cmd/Ctrl+Z` 快速撤回。
- 滚动条默认显示得更细，悬停时恢复原有宽度，降低界面干扰。

### Fixed

- 修复视觉索引向量化仍在主进程内执行、导致应用交互明显卡顿的问题，现改为独立后台进程处理。
- 修复主界面分栏拖拽热区压到滚动条边缘的问题，并微调滚动条宽度，避免右侧区域难以直接拖动滚动条。
- 修复素材区意外出现横向滚动条和滚动条角白块的问题，素材列表现在只保留纵向滚动。
- 修复切换素材库后偶发只更新标题路径、实际仍停留在旧数据库，导致缩略图和预览同时失效的问题。
- 修复文件夹拖拽排序时容易误判为嵌套的问题，并让同级排序和跨层移动后的顺序都能稳定保存。
- 修复右侧元信息输入框的编辑体验：macOS 下支持 `Cmd+A/C/V` 全选、复制、粘贴，其他平台兼容 `Ctrl+A/C/V`，并关闭拼写检查带来的红色波浪线。
- 修复应用内通用复制粘贴快捷键缺失的问题，恢复原生 `Cmd/Ctrl+C/V/X/A` 编辑菜单，并让外部复制的图片可直接粘贴导入应用。
- 修复应用内图片粘贴与拖入仍先转成 `base64` 的问题，改为直接按图片二进制内容导入和落盘。
- 修复应用内复制后再粘贴图片时会丢失评分、描述、来源链接、标签和原始图片格式的问题。
- 修复根目录新建文件夹时缺少显式入口的问题，并在创建前拦截重名文件夹。
- 修复已展开筛选栏时点击素材会立刻收起面板、造成筛选像被清空的误导体验。
- 修复从应用内回收站恢复素材时，若原文件夹已不存在，素材会自动恢复到“未分类”而不是落入失效路径。
- 修复恢复已删除文件夹时，若原父目录缺失会错误落到素材库根目录的问题，改为自动补齐缺失目录后按原路径恢复。
- 修复已删除文件夹恢复时遇到同名目标会发生路径冲突的问题，改为自动恢复为“(已恢复)”副本。
- 修复文件先进入回收站、随后所在文件夹再进入回收站时，单独恢复该文件可能丢失的问题，改为恢复到“未分类”并同步扣减文件夹条目计数。

## [0.3.2] - 2026-04-22

### Added

- 素材目录支持运行中实时同步，并在应用启动或重新聚焦时后台校准外部增删改名。
- 左侧栏新增快捷视图入口，可直接切换“未分类”“未标签”“最近使用”和“随机模式”视图。

### Changed

- 应用图标打包进应用。
- 关闭主窗口后改为继续常驻系统托盘或菜单栏，浏览器采集扩展依赖的本地 HTTP 服务会保持可用，并减少后台空闲占用。
- 设置页改为更轻量的分隔式布局，减少说明文字和嵌套卡片。
- 左侧栏导航与文件夹列表统一为同一套紧凑样式，减少分割线和块状区隔。
- 标签管理页改为更克制的双栏结构，移除“全部标签”、快捷操作和多层卡片，常用操作收回到右键菜单。
- `PDF` 和 `PSD` 在应用内预览时统一显示快照缩略图，并在预览栏标记“快照缩略图”。
- 外部删除素材时不再立即丢弃素材元数据，后续文件恢复或移动识别后可继续保留评分、描述和标签。
- 缩略图策略收敛为单一 768px WebP 缓存，列表卡片与详情侧栏共用同一张缩略图，并统一写入 `.shiguang/thumbs/`。
- `PDF` 和 `PSD` 缩略图改为应用内自带渲染依赖，打包后可直接使用，无需用户额外安装系统工具。

### Fixed

- 双击标签时改为直接进入“全部素材 + 标签筛选”结果，并收紧切换顺序，减少页面切换时的闪动感。
- 修复选中文件夹后左侧“全部素材”仍保持高亮的问题。
- 仅为视频、超大图片和达到阈值的素材生成缩略图，普通小图直接回退原图，减少不必要的缓存和重复生成。
- 切换文件夹后素材卡片改为优先加载当前可见区域的真实图片，并收紧预热范围，减少长时间停留在 ThumbHash 占位图和只有前几张先变清晰的情况。
- 启动骨架屏和文件夹切换中间态移除“恢复上次位置”“正在加载文件”等无关提示文案，减少内容切换时的视觉干扰。
- 导入与重建索引后的缩略图生成改为后台异步执行，并基于内容哈希复用缓存，降低主流程阻塞和重复转码。
- 修复开发模式下移除菜单栏后按 `F12` 无法打开开发者工具的问题。
- 修复发布脚本更新版本号时遗漏 `package-lock.json`，避免发布提交中的 npm 锁文件版本不同步。
- 修复素材外部拖拽时图标异常偏大，并限制详情预览页不再支持拖拽到文件夹或应用外部。
- 修复素材卡片缺少内部拖拽源，恢复拖拽到文件夹时的移动操作。
- 修复单张素材在网格和自适应视图中过度放大占满内容区的问题，改为限制宽度并居中展示。
- 修复素材列表快速滚动时缩略图加载滞后，减少滚动后长时间显示占位图的情况。
- 为素材卡片增加 ThumbHash 占位图，并在旧素材首次显示时自动补齐，减少快速滚动时的黑块和空白感。
- 修复素材卡片加载中 ThumbHash 占位图被文件类型占位覆盖的问题。
- 调整素材列表的虚拟窗口和预热策略，优先保留当前滚动方向附近的缩略图，减少快速上下滑动时反复回退到占位图。

## [0.3.1] - 2026-04-21

### Added

- 新增 Electron 桌面运行时，素材库、导入、扫描、缩略图、文件夹、标签、回收站、浏览器采集和 AI 元数据分析可在 TypeScript 后端中运行。
- 新增 Electron 版本本地 `chinese_clip` 自然语言搜图链路，支持模型目录校验、CPU 推理、视觉索引建立、导入后自动向量化和文本搜图。
- 本地自然语言搜图新增 `fg_clip` 模型目录支持，可使用 FG-CLIP2 建立视觉索引和执行文本搜图。
- 本地自然语言搜图新增平台 GPU 加速选择，Windows 可使用 DirectML，Apple Silicon 可使用 CoreML；运行时保持懒加载，不影响应用启动速度。

### Changed

- 桌面端构建发布链路切换为 npm、electron-vite、Vite 8、Electron 和 electron-builder。
- 本地自然语言搜图状态页现在会显示模型校验、运行时加载状态，以及已索引、失败、待处理、过期图片数量。
- 发布脚本现在会把 `Unreleased` 变更整理为新版本 changelog，并写入 GitHub Release 说明。

### Fixed

- 隐藏 Windows 默认菜单栏，并修正桌面窗口与拖拽使用的应用图标资源。
- 修复 FG-CLIP2 处理小尺寸图片时 patch bucket 和位置向量长度不一致导致索引失败的问题。
- 修复 Electron 发布页下载链接与实际产物不一致的问题，并在发布流程中校验平台原生推理依赖。
- 修复 Linux `.deb` 打包缺少维护者邮箱导致发布流程失败的问题。

### Removed

- 移除前端运行时对 Tauri IPC、文件系统、日志和对话框插件的依赖。
- 移除 Tauri/Rust 后端和对应的构建发布流程。
